# End-to-End-CICD-implementation-project
This project implements an end-to-end CI/CD pipeline for a Java-based application. Jenkins automates the build, test, and deployment stages, with the application containerized using Docker and deployed to a Kubernetes cluster.
